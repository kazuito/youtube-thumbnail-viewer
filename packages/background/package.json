{
  "name": "background",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild --bundle src/index.ts --outfile=../../dist/background.js --platform=browser --target=chrome105 --sourcemap=inline --watch",
    "build": "esbuild --bundle src/index.ts --outfile=../../dist/background.js --platform=browser --target=chrome105 --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.7.0",
  "devDependencies": {
    "@types/chrome": "^0.0.316",
    "esbuild": "^0.25.2"
  }
}
